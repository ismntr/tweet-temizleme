import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Allow LAN access for mobile testing (Add your local IP here)
  // allowedDevOrigins: ["192.168.1.103", "localhost"],
};

export default nextConfig;
